{
  "name": "preflight",
  "version": "0.4.0",
  "description": "Local-only web-assurance scaffolding for any web project. Runs Playwright (Chromium/Firefox/WebKit) plus axe-core, accessible-name sweep, real-NVDA (via Guidepup on --release, opt-in), Lighthouse budgets, html-validate, and lychee link-check against your dev server, with no SaaS dependency.",
  "license": "MIT",
  "author": "preflight contributors",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "bin": {
    "preflight": "bin/preflight.mjs"
  },
  "files": [
    "bin",
    "dist",
    "templates",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "scripts": {
    "prepare": "tsc -p tsconfig.build.json",
    "build": "tsc -p tsconfig.build.json",
    "test": "preflight"
  },
  "peerDependencies": {
    "@playwright/test": ">=1.50.0"
  },
  "dependencies": {
    "@axe-core/playwright": "^4.10.0",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@guidepup/guidepup": "^0.24.1",
    "@guidepup/playwright": "^0.15.0",
    "@guidepup/setup": "^0.21.0",
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.10.0",
    "html-validate": "^11.4.0",
    "lighthouse": "^13.3.0",
    "playwright-lighthouse": "^4.0.0",
    "typescript": "^5.7.0"
  },
  "keywords": [
    "playwright",
    "axe",
    "accessibility",
    "a11y",
    "wcag",
    "smoke-test",
    "web-assurance",
    "preflight"
  ]
}
