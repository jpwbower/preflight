#!/usr/bin/env node
// preflight CLI entry — plain JS so it runs without a TS loader.
// Full implementation lands in Commit 4.
process.stderr.write('preflight CLI skeleton — implementation pending\n');
process.exit(4);
